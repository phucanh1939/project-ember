namespace Game.Gameplay
{
    public enum StateInterruptType
    {
        Stun,
        Knockback,
        Death,

        Count // IMPORTANT: Must remain the last enum value. Used as the number of interrupt types.
    }
}
